import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  fab: {
    position: 'absolute',
    margin: 16,
    right: 0,
    bottom: 0,
    backgroundColor: "dodgerblue"
  },

  picker: {
    marginTop: 4,
    borderWidth: 1,
    borderColor: 'grey',
    borderRadius: 4,
  },
});